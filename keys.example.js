// Copy this file to `keys.js` (do NOT commit `keys.js`).
// Replace the placeholder with your restricted browser API key.
window.APP_KEYS = {
  BROWSER_KEY: 'REPLACE_WITH_BROWSER_KEY'
};

// Example for future server key (if you add a server later):
// window.APP_KEYS.SERVER_KEY = 'REPLACE_WITH_SERVER_KEY';